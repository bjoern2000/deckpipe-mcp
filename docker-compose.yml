services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: deckpipe
      POSTGRES_USER: deckpipe
      POSTGRES_PASSWORD: deckpipe
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
